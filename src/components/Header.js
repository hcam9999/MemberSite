const header = ({title}) => {
    return (
        <header className="header">
                <h1 className="header--h1">{title}</h1>
                
        </header>
    )
}

export default header
